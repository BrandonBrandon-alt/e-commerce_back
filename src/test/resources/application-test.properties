# Test Configuration
spring.application.name=e-commerce_back-test

# ===============================
# H2 In-Memory Database for Tests
# ===============================
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA / Hibernate for Tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=false

# ===============================
# JWT Configuration for Tests
# ===============================
app.jwt.secret=testSecretKey123456789012345678901234567890
app.jwt.expiration=3600000

# ===============================
# Disable Redis for Tests
# ===============================
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

# ===============================
# Email Configuration for Tests
# ===============================
# Gmail SMTP Configuration (SSL Alternative)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=brandonmontealegre15@gmail.com
spring.mail.password=poyc sejz feln yhut
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Email Templates Configuration for Tests
app.email.from-name=E-Commerce Store (TEST)
app.email.from-address=brandonmontealegre15@gmail.com
app.email.activation-code-expiry-minutes=15
app.email.reset-password-code-expiry-minutes=10

# ===============================
# Logging for Tests
# ===============================
logging.level.com.e_commerce=DEBUG
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.springframework.mail=DEBUG
logging.level.javax.mail=DEBUG

# ===============================
# Disable Actuator for Tests
# ===============================
management.endpoints.enabled-by-default=false
