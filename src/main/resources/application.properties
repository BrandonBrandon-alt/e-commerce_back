spring.application.name=e-commerce_back

# ===============================
# Server
# ===============================
server.port=8080

# ===============================
# PostgreSQL
# ===============================
spring.datasource.url=jdbc:postgresql://host.docker.internal:5433/ecommerce_db
spring.datasource.username=ecommerce_user
spring.datasource.password=c@ntraseña123
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Settings (HikariCP)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# ===============================
# Redis Configuration
# ===============================
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.database=0
spring.data.redis.timeout=5000ms

# Redis connection pool (Lettuce)
spring.data.redis.lettuce.pool.max-active=10
spring.data.redis.lettuce.pool.max-idle=5
spring.data.redis.lettuce.pool.min-idle=2
spring.data.redis.lettuce.pool.max-wait=3000ms
spring.data.redis.lettuce.shutdown-timeout=100ms

# ===============================
# JWT Configuration
# ===============================
app.jwt.secret=mySecretKeyForHS512AlgorithmMustBeAtLeast64BytesLongToWorkProperly123456
app.jwt.expiration=3600000
app.jwt.refresh-expiration=604800000
app.security.max-concurrent-sessions=3

# ===============================
# Security Configuration
# ===============================
app.security.max-failed-attempts=5
app.security.lockout-duration-minutes=15
app.security.unlock-code-expiry-minutes=15
app.security.refresh-token-expiry-days=7
app.security.email-verification-expiry-hours=24

# ===============================
# Email Configuration
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=brandonmontealegre15@gmail.com
spring.mail.password=poyc sejz feln yhut
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email Templates Configuration
app.email.from-name=E-Commerce Store
app.email.from-address=brandonmontealegre15@gmail.com
app.email.activation-code-expiry-minutes=15
app.email.reset-password-code-expiry-minutes=10

# ===============================
# Swagger / OpenAPI
# ===============================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# ===============================
# Logging
# ===============================
logging.level.com.e_commerce.e_commerce_back=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===============================
# Actuator (Monitoring)
# ===============================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized